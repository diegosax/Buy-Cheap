<%= form_for(@big_order) do |f| %>
  <% if @big_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@big_order.errors.count, "error") %> prohibited this big_order from being saved:</h2>

      <ul>
      <% @big_order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
